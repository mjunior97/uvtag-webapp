<div class="navbar">
    <a href="/Logout">Logout</a>
    <a href="/Perfil">Perfil</a>
    <a href="/Eventos">Eventos</a>
    <a href="/Selecionar">Trocar Dispositivo</a>
    <h2>UV TAG - MONITOR</h2>
</div>

<form action="/Monitor" method="POST">
    <br><br><br>
    <div align="center">
        <label>Data de: </label>
        <input class="button2" type="date" name="data_from">
        <label>Data até: </label>
        <input class="button2" type="date" name="data_to">
        <button class="button2" type="submit">Pesquisar</button>
    </div>
</form>

<br>
<div align="center">
    <div>
    <table width="100%" align="top">
        <td>
            <h3 align="center">Leituras do Período</h3>
            <p align="center">Se nenhum período for selecionado, é recuperado o mês atual</p>
            <table class="tabler">
                <tr>
                    <td>id</td>
                    <td>Controlador</td>
                    <td>Dispositivo</td>
                    <td>Índice UV</td>
                    <td>Dose (mW/m²)</td>
                    <td>Distância (cm)</td>
                    <td>Data</td>
                    <td>Hora</td>
                </tr>

                {{#each posts}}
                    <tr>
                        <td>{{dataValues.id_leitura}}</td>
                        <td>{{dataValues.controlador}}</td>
                        <td>{{dataValues.dispositivo}}</td>
                        <td>{{dataValues.indice}}</td>
                        <td>{{dataValues.dose}}</td>
                        <td>{{dataValues.distancia}}</td>
                        <td>{{dataValues.data}}</td>
                        <td>{{dataValues.hora}}</td>
                    </tr>
                {{/each}}
            </table>
        </td>
        <td>
            <h3 align="center">Médias do Período</h3>
            <table class="tabler" align="top">
                <tr>
                    <td>Índice UV</td>
                    <td>Dose (mW/m²)</td>
                    <td>Risco</td>             
                </tr>
                <tr>
                    <td>{{avg_indice}}</td>
                    <td>{{avg_dose}}</td>
                    <td>{{risco}}</td>
                </tr>
            </table>
        </td>
    </table>
</div>